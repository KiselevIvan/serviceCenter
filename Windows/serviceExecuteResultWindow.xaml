<Window x:Class="serviceCenter.Windows.serviceExecuteResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:serviceCenter.Windows"
        mc:Ignorable="d"
        Height="300" Width="600"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Label Content="Опишите результат выполнения услуги:" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,10,0,0"/>
        <TextBox Name="tbResult" Grid.Row="1" Margin="20,0,20,0"/>
        <StackPanel Orientation="Horizontal" Margin="0,20,0,20" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" >
            <Button Name="bOk" Content="Сохранить" Click="bOk_Click" Margin="0,0,20,0"/>
            <Button Name="bCancel" Content="Отмена" Click="bCancel_Click" Margin="20,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>
