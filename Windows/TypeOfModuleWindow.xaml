<Window x:Class="serviceCenter.Windows.TypeOfModuleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:serviceCenter.Windows"
        mc:Ignorable="d"
        Height="130" Width="300"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Label Content="Добавление нового типа устройства" Grid.ColumnSpan="2" HorizontalContentAlignment="Center"/>
        <Label Grid.Row="1" Content="Название:"/>
        <TextBox Grid.Row="1" Grid.Column="1" Name="tbName" MaxLength="60"/>
        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button  Name="bOk" Content="Добавить" Click="bOk_Click" Margin="0,0,10,0"/>
            <Button  Name="bCancel" Content="Отмена" Click="bCancel_Click" Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>
